<!--$("#<%#= dom_id(@follow_request) %>").fadeOut(1000, function() {
  $(this).remove();
});-->

$("#<%= dom_id(@follow_request) %>").replaceWith(
  "<%= j(render "follow_requests/form", follow_request: @other_user.received_follow_requests.build) %>"
  );
